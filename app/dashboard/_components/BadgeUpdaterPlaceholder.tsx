'use client'

import { useEffect } from 'react'

/**
 * Placeholder for the upcoming useBadgeUpdate hook (Task54).
 * Keeps the component tree ready without implementing the future task.
 */
export function BadgeUpdaterPlaceholder() {
  useEffect(() => {
    // noop for now – real badge integration will plug in here later
    return () => {}
  }, [])

  return null
}
